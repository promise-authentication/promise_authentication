<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= name %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="Description" content="<%= t '.meta_description', name: name %>">

    <meta name="viewport" content= "width=device-width, initial-scale=1.0"> 
    <link rel="icon" type="image/png" sizes="180x180" href="<%= image_url 'inverted_logo.png' %>">

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
      <% if unique_color %>
        .button.primary {
          background-color: #<%= unique_color %>;
          opacity: 0.9;
        }
        .button.primary:hover {
          background-color: #<%= unique_color %>;
          opacity: 1;
        }
        .bg-uniq {
          background-color: #<%= unique_color %>;
        }
        .text-uniq {
          color: #<%= unique_color %>;
        }
      <% end %>
    </style>
  </head>
  <body class="">
    <%= content_for?(:content) ? yield(:content) : yield %>

    <% if unique_character %>
      <%= link_to unique_character, uniq_path, class: "mr-3 mb-3 text-white font-mono text-xs font-black fixed bottom-0 right-0 bg-uniq rounded-full h-6 w-6 flex items-center justify-center shadow-lg", style: 'cursor:help;', 'aria-label': "Your unique letter/number-color combination is #{unique_character} and #{unique_color}" %>
    <% end %>
  </body>
</html>
