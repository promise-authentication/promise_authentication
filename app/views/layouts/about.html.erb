<%= content_for :content do %>
  <%= render partial: 'about/navigation' %>
  <div class="px-4 pt-5 m-auto <%= current_page?(action: 'comparison', controller: '/about') ? '' : 'max-w-2xl' %>">
    <div class="prose mb-12 mt-12">
      <%= yield %>
    </div>
  </div>

  <hr class="w-2/3 m-auto mb-10 mt-8"/>

  <%= render partial: 'shared/footer' %>
<% end %>

<%= render template: "layouts/application" %>
